import { CompletenessPipe } from './completeness.pipe';

describe('CompletenessPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletenessPipe();
    expect(pipe).toBeTruthy();
  });
});
